version: 2

models:
  - name: int_model_1
    description: Super basic model that has a description and a test
    columns:
      - name: id
        tests:
          - values_below_threshold:
              threshold: 10
          - not_null
