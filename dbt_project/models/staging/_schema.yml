version: 2

models:
  - name: stg_model_1
    description: Super basic model that has a description and a test
    columns:
      - name: id
        tests:
          - not_null

  - name: stg_model_2
    description: Super basic model that has a description and a test
    columns:
      - name: id
        tests:
          - unique
