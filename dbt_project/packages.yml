packages:
  - package: metaplane/dbt_expectations
    version: [">=0.10.0", "<0.11.0"]
  # `metricflow_time_spine` calls `dbt_utils.date_spine`. The package already arrived
  # through `dbt_expectations`, but a model must not depend on a transitive package.
  - package: dbt-labs/dbt_utils
    version: [">=1.4.0", "<2.0.0"]
  - package: fivetran/tiktok_ads
    version: [">=1.1.0", "<2.0.0"]
  - local: ./internal_package
