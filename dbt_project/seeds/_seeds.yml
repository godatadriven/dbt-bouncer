version: 2

seeds:
  - name: raw_customers
    description: Raw customer data
    columns:
      - name: id
        data_type: integer
      - name: first_name
      - name: last_name

  - name: raw_orders
    description: Raw order data
    columns:
      - name: id
        data_type: integer
      - name: user_id
        data_type: integer
      - name: order_date
        data_type: date
      - name: status

  - name: raw_payments
    description: Raw payment data
    columns:
      - name: id
        data_type: integer
      - name: order_id
        data_type: integer
      - name: payment_method
      - name: amount
        data_type: integer
